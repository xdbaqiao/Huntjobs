<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huntjobs.android"
    android:versionCode="1"
    android:versionName="1.0" >
    <uses-sdk android:minSdkVersion="14" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <application
        android:icon="@drawable/huntjobs"
        android:label="@string/app_name" >
        <activity
            android:name=".HuntjobsActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NorthActivity" android:label="@string/northic">    
        </activity> 
        <activity android:name=".SouthActivity" android:label="@string/southic">    
        </activity> 
        <activity android:name=".AboutActivity" android:label="@string/abouttitle">    
        </activity> 
        <activity android:name=".DetailActivity" android:label="@string/detail">    
        </activity> 
        <activity android:name=".XJActivity" android:label="@string/xajtic">    
        </activity> 
        <activity android:name=".NwpuActivity" android:label="@string/nwpuic">    
        </activity>
    </application>
</manifest>